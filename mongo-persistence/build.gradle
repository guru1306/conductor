dependencies {

    compile project(':conductor-core')   
    compile "com.google.inject:guice:${revGuice}"
    compile "org.mongodb:mongodb-driver-sync:${revMongoClient}"
    compile project(':conductor-redis-persistence')
    
}